name: k3s Testing
on:
  push:
    branches:
      - 'feature/**'
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - run: |
        pwd
        ls -lrt
        docker build -f ../tests/Dockerfile -t test  .
        docker images
    - uses: debianmaster/actions-k3s@v1.0.3
      id: k3s
      with:
        version: 'latest'
    - run: |
        docker images
        kubectl run --image=test test

