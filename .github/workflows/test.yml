name: k3s Testing
on:
  push:
    branches:
      - 'fix/**'
      - 'master'
      - 'main'
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: debianmaster/actions-k3s@v1.0.1
      id: k3s
      with:
        version: 'v0.9.1'
    - run: |
        docker ps -a
        sleep 3
        cat /tmp/output/kubeconfig-v0.9.1.yaml
        kubectl get nodes

