name: k3s Testing
on:
  push:
    branches:
      - 'fix/**'
      - 'master'
      - 'main'
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: debianmaster/actions-k3s@v1.0.1
      id: k3s
      with:
        version: 'latest'
    - run: |
        docker ps -a
        sleep 3
        docker logs -f latest
        cat /tmp/output/kubeconfig-latest.yaml
        kubectl get nodes

