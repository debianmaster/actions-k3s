name: k3s Testing
on:
  push:
    branches:
      - 'fix/**'
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: debianmaster/actions-k3s@fix/depricated-set-env
      id: k3s
      with:
        version: 'v0.9.1'
    - run: |
        kubectl get nodes
